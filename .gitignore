# Dependencies
tests/node_modules/

# Raw pipeline sources — regenerable via data-pipeline/download.sh
# (the compiled extension/data/dictionary.json IS committed)
data-pipeline/raw/

# OS / editor cruft
.DS_Store
*.swp

# Build output — regenerate with ./package.sh
dist/

# Test verification screenshots — regenerated by tests/browser-verify.js
# (the curated store images in docs/store-assets/ ARE tracked)
tests/screenshots/

# Scratch
*.log
